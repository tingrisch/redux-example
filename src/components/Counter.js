import React from "react";
import styled from "styled-components";

const CounterDiv = styled.div`
    padding: 1rem;
    margin: 1rem;
`;

const Counter = () => 
    <CounterDiv>0</CounterDiv>;

export default Counter;